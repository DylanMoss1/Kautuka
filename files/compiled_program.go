package main

import (
	"fmt"
)

func main() {
	// Default
	// Default
	for i := 1; i <= 3; i++ {
		// Default
		fmt.Println(i)
	}
	// Default
	for j := 7; j <= 9; j++ {
		// Default
		fmt.Println(j)
	}
	// Default
	for n := 0; n <= 5; n++ {
		// Default
		if n == 0 {
			// Default
			continue
		}
		fmt.Println(n)
	}
}
